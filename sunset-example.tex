\documentclass{article}
\usepackage[]{geometry}
\usepackage{amsmath}
\usepackage{correlator-diagrams}

\begin{document}

\section*{Sunset Diagram Example}

Scalar sunset self-energy with external momentum \(p\):
\[
  \SunsetDiagram[
    scale=1.2,
    field=\phi,
    sunset-external-momentum=p,
    sunset-top-momentum=k,
    sunset-middle-momentum=p-k-q,
    sunset-bottom-momentum=q,
  ]
\]

The same macro also supports different labels and momentum assignments, for example:
\[
  \SunsetDiagram[
    scale=1.2,
    field=\phi,
    show-momenta=false
  ]
\]

\end{document}
